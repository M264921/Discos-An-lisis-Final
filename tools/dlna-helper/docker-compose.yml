version: "3.8"

services:
  dlna-helper:
    image: node:20-alpine
    working_dir: /app
    command: ["node", "server.js"]
    environment:
      - PORT=8787
      - HOST=0.0.0.0
      - DLNA_DEVICES=${DLNA_DEVICES:-}
    volumes:
      - .:/app
    ports:
      - "8787:8787"
